.newest-comments.row
  .col-md-1
  .col-md-10
    %h4 Newest comments:
    %table.table.table-striped
      %thead
        %tr
          %th #
          %th Text
      %tbody
        - project.comments.order("created_at DESC").each do |c|
          %tr
            %td= c.id
            %td= c.text
  .col-md-1
